spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Altere estas linhas para usar valores genéricos ou mocks
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:9999/mock
# Remova ou comente a linha do issuer-uri abaixo se o erro persistir,
# pois ela força o Spring a tentar conectar na rede
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/grafica

spring.main.allow-bean-definition-overriding=true